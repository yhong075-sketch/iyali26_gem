[project]
name = "iyali26"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cobra>=0.30.0",
    "gurobipy>=13.0.1",
    "memote>=0.17.0",
    "pre-commit>=4.5.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.7",
]
