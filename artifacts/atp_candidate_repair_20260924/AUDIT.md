# ATP 候选修复独立结果审核

审核者：`/root/atp_result_audit`；2026-09-24。已读取本轮完整授权、AGENTS.md、PROJECT_STATE.md、原始 XML、当前构建/求解源码及保存交付物。审核者未调用优化器、未修改模型或构建代码。`auditor_check.py` 为独立纯标准库核验，不调用生产加载器或候选补丁函数。

**计算结论：最终 E5 在本轮完整封闭模型的 ATP、GTP、UTP、CTP 检查均报告 optimal、最大耗散0；正常 PO1f/SD-Leu 条件仍可生长。** 这支持本轮四类闭合能量验收，不代表全模型化学、原生功能、所有能量形式或热力学正确，也不是正式替换基线的接纳决定。核验覆盖58次实际调用的全部134,224个通量值及完整运行约束；最优性依据保存的求解器状态和生产代码，审核者没有独立重求最优解或对偶证书。

## 输入、候选范围与默认行为

原始基线 SHA256 仍为 `aad701126d12d113816fda4b872333b614b4469ee1b6d8ab8c419231c89e965f`，2314反应、1877物种。纯XML解析独立比较E0–E5全部反应、物种、目标和GPR。所有2314条GPR、名称、反应集合及目标相同；候选仅改变以下指定字段：

| 版本 | 相对E0的反应改变 |
|---|---|
| E0 | 无反应结构/边界改变 |
| E1 | PGAM1_PhosHydro、NTP3pp禁存储正向；NTP7禁存储逆向 |
| E2 | NTP3pp去反应物H+；NTP7去产物H+；r0242增加产物2H+ |
| E3 | E1、E2并集 |
| E4 | E3加NDP1去反应物H+并禁正向；R72禁逆向 |
| E5 | E4加CAT2p禁逆向；OAADCm去产物H+并禁正向 |

表中省略前缀的ID在完整记录中分别为R_PGAM1_PhosHydro、R_NTP3pp、R_NTP7、R_NDP1、R_CAT2p、R_OAADCm。八条E5已修改反应的存储元素、电荷均由独立公式求和核实为零残差，见`auditor_final_chemistry.json`；未改任何共享物种公式。此为存储表示配平，不是原生酶学验证。

原始XML与候选重导出存在一个明确的非字节等价细节：20个`trna_biomass_residue_*`物种原无`fbc:charge`，重导出写为0。COBRA原读入时即默认0，加载后物种定义和本轮优化问题保持，但原始缺失值不能解释为已知中性或新化学证据。其他原始物种名称、公式、区室及电荷无差异；详细20项保存在`auditor_results.json`。因此E0指加载后定义不变，不能声称E0与正式XML字节相同。

默认开关E0不调用候选修订，原始输入保留。独立入口与主构建最后阶段调用同一候选函数；本轮完成的是完成态参考的最终候选阶段及限定范围重放，未重建全部历史环境。默认完整构建未从头运行，不能把默认no-op测试称完整历史重现。

## 覆盖根因与保护机制

已读实际35份阶段快照、140个字段行及重放脚本。真实gap-fill函数、charge-aware balance后，metadata selection首次把PEP/GTP水解方程反向、恢复三个双向边界，并重新引入NTP3pp/NTP7多余H+、删去r0242的2H+。字段来自`data/metadata_reaction_selection.json`明确选择的旧`stoichiometry`/`bounds`，不是名称更新造成。R2010五列限定重放中保持。

当前机制先全体检查确切反应/物种签名，再绝对赋值，持久化完整保护定义。metadata编辑前先验证全部锁，并保留受保护化学和边界；这不是诊断末尾临时改回。实际重放中再次metadata及导出/重载后候选定义保持。源码还拒绝重写已有输出、额外自定义约束、被改动稳态行界，并比较导出前后完整求解器定义。测试覆盖默认no-op、幂等、累计与直接修订一致、禁止退级、字段/物种冲突原子失败、锁被篡改、约束/矩阵修改拒绝。

## 完整运行问题与58份解

按稳定ID从E0运行态和每个XML的确切候选差异独立构造每版应有运行态：所有GPR、培养交换界、菌株条件、biomass_C目标及NGAM边界保持。闭合再独立识别183条全部单侧source/sink/exchange及6条biomass，共189列固定零，唯一其余基线界变更为xMAINTENANCE下界7.8625→0。全部1877物种行保留且上下界0；无额外强制约束。所有版本均实际进行零向量LP；不只用medium为空判断封闭。

逐份核对保存的全反应定义、干预边界、原生稀疏矩阵、目标式、solver参数、稳态和变量界。原复制会使R1372的矩阵系数最多舍入`3.603044599973643e-11`；按变量ID对齐后所有其他系数一致。R1372在闭合及保存真实培养解均为0，故这一已披露差异不影响已核见证。全部物料及原生行残差远低于1e-7，边界违反0；L1净通量重算与记录目标的最大差在浮点量级。具体每组最大值见`auditor_results.json`。

- 初始E0–E3验证：19次；E3全模型方向干预：5次。
- E4验证：7次；E4全模型方向干预：5次。
- 最终E5验证：6次；两项隔离真实培养归因：2次。
- 初始/最终行为测试：6+8次，含E3剩余异常负控制及E5重复闭合ATP/真实培养正控制。

58条唯一budget记录与所有保存结果一一对应，无旧LP复用。全部optimal、后处理complete；本轮没有失踪、infeasible或失败求解被填0。实际`optimize()`调用累计墙钟`1.183404672308825 s`，远小于1800 s；这不等于总任务墙钟。每次记录1线程、60 s、Presolve0和1e-7容差，未放宽。生产封装在异常finally中也计时，预算不足下一完整60 s时拒绝新调用。

## 见证、方向干预及最终能量

两份D=1见证的31个非零反应展示行和44个代谢物账本行全部独立重算；逐项包含H+、水、Pi、核苷酸、氧化还原物及全部中间体。按保存符号通量求和满足`Σ(j≠D)S_j v_j = −S_D v_D`。总式配平不掩盖单列缺式或失衡。L1是总绝对通量最小化，不证明最少反应数。

E3的新路径包含R_NDP1无耦联合成ADP、R121逆向及肌醇磷酸回路。E4见证转至涉及R_CAT2p逆向、R_OAADCm正向、氧化还原和跨区室连接的19列路径。R_CAT2p准确方程为ethanol[cy]+H2O2[pe]→acetaldehyde[cy]+2H2O[pe]；它不是产生O2的catalase dismutation。审核在E5优化前指出了初稿错误rationale，旧稿独立保留，最终E5使用更正定义和说明。原生酶活与混合区室合理性仍由来源证据单独限制。

完整模型干预保持D[0,1000]并移除D=1/L1条件。E3禁NDP1正向仍489.6551724；仅禁R72逆向仍1000。E4仅禁CAT2p逆向ATP为0，仅禁OAADCm正向仍241.3793103，联合为0。这些临时数学干预与证据支持的候选修订分别列账；未把正常R694/R594/R603删除，也未将其他见证成员自动关闭。

| 版本 | ATP最大值 | GTP/UTP/CTP最大值 |
|---|---:|---:|
| E0 | 1000 | 均1000 |
| E1 | 1000 | 未执行 |
| E2 | 1000 | 未执行 |
| E3 | 1000 | 均1000 |
| E4 | 489.655172413793 | 各约489.655172413793 |
| E5 | 0 | 均0 |

全部状态optimal。1000表示有限测试容量上限，不表示数学无界。新增三NTP耗散使用当前中性NTP/NDP/Pi形式，四种耗散均独立元素/电荷配平；原生ATP维护列保持原式。E5停止条件是四项实际检查到0；未穷举全部能量形式或所有内部循环。

## 真实培养与测试

E0–E4生长均约1.87188230694 h⁻¹；最终E5为1.429177892279185 h⁻¹，且最终行为测试重新求解再现。全部维持同一培养约束、biomass_C最大化与NGAM[7.8625,1000]，没有为了恢复旧数字改变培养、biomass或维护式。

E4+仅CAT2p修订生长为1.4299514166082394，E4+仅OAADCm修订为1.8718823069403032。与最终E5比较表明本组修订的生长下降主要随禁止CAT2p逆向出现，另有小的组合影响；这不是CAT2p在天然酵母中的因果实验或唯一生理限速证明。E3保存的一个最优解维护通量66.312714834907，而其他主版本为7.8625；边界相同不意味着最优解耗散必相同，不将任意最优解额外耗散视为真实需求。

最终`final_tests.log`显示23项测试通过。它包括实际优化行为检查（均纳入58预算）及构建/相关既有回归。早期阶段重放和构建测试软件失败保留，未作为成功结果；最终静态guard不因复制舍入而放宽容差，而使用与真实入口一致的新读XML。

## 保护与审核覆盖

最终核验`before.json`中452项预先保护文件：448项SHA相同，4项为授权修改的README、cli.py、main.py、reaction_selection.py，无缺失。PROJECT_STATE不在该452项列表内，另以实际`before/PROJECT_STATE.md`快照核对，确为第五项授权文档变更；合并去重范围因此为453文件，448不变、5项授权变更。原模型、培养、整理的旧数据、GPR及其他未提交科研文件保持；分支与HEAD仍为原值。保护范围以`before.json`和`auditor_protection.json`为准，不宣称整个历史dirty环境被重建或恢复。

| Claim组 | 范围 | 判定 |
|---|---|---|
| C1 | 固定输入/源身份和当前基线 | supported |
| C2 | 默认no-op、原始输入保护及字节/语义区别 | supported |
| C3 | 运行时字段覆盖根因与持久优先级机制 | supported |
| C4 | E0–E5精确XML范围、GPR不变及八列存储配平 | supported |
| C5 | 闭合边界、稳态行和原生矩阵 | supported |
| C6 | 58调用完整向量、参数、目标、预算与异常分类 | supported |
| C7 | 31见证反应及44完整净账本行 | supported |
| C8 | 单独方向干预、E5四能量结果与有限范围 | supported |
| C9 | 原培养恢复、生长与两项有限归因 | supported |
| C10 | 默认/幂等/冲突/roundtrip及实际优化测试记录 | supported |

计算审核覆盖：**10 total | 10 audited | 10 supported | 0 unresolved | 0 contradicted | 0 unchecked**，均限上述可检验声明。来源审核另见`chemistry/REPORT.md`（25条，22支持、3未决），不得把本计算审核覆盖数理解为全部生物学已验证。未决原生功能、区室、剩余未配平列与未测试的能量/热力学性质限制正式生物学接纳，不否定已核实的本轮四类闭合能量和生长计算结果。

最终主报告已逐段核查，核心版本、修订范围、能量结果、生长归因及声明局限与以上记录相符。项目状态追加后的保护核验已完成；报告追加收口时间/链接不改变科学判断。
